# Resend API key — get one at https://resend.com/api-keys
RESEND_API_KEY=

# Verified sender address (must belong to a domain verified in Resend).
# Falls back to onboarding@resend.dev for local testing if unset.
CONTACT_FROM_EMAIL=info@bws-bennet.com
